package vetores03;

import java.util.Locale;
import java.util.Scanner;

public class Program {
    public static void main(String[] args) {
        Locale.setDefault(Locale.US);
        Scanner sc = new Scanner(System.in);

       /* System.out.print("Entre com a quantidade de pessoas: ");
        int pessoas = sc.nextInt();

        double[] vect = new double[pessoas]; //Declaração do vetor + Quantidade de posições = pessoas;

        double somatoria = 0;
        for (int i = 0; i < vect.length; i++) {
            System.out.print("Entre com a altura: ");
            vect[i] = sc.nextDouble();
            somatoria += vect[i];
        }

        for (int i = 0; i < vect.length; i++) {


        }
        double media = somatoria / pessoas;
        System.out.printf("MÉDIA DE ALTURA: %.2f", media);
*/
    }
}
